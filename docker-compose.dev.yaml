services:
  app:
    build: .
    container_name: ai-goofish-monitor-app
    init: true
    ports:
      - "5352:5352"
    env_file:
      - .env
    volumes:
      - .:/app
      - /app/dist
    restart: unless-stopped
